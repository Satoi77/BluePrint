# 蓝图可视化系统 — 实施步骤文档 v1.0

> 版本：v1.0（MVP 施工单）
> 配套：`docs/工程设计文档_v1.0.md`
> 日期：2026-09-13
> 说明：每阶段完成后将 `- [ ]` 改为 `- [x]` 并注明日期。每阶段有明确"验收"，未过验收不进入下一阶段。
>
> **完成状态（2026-09-13）**：阶段 0–4、6–10 全部完成；阶段 5 除"800+ 文件大项目计时"（待用户提供目标项目路径）外完成。pytest 23/23 通过，`npm run build` 通过，原生 Edge 端到端验证 7 节点/4 连线/无报错。

---

## 阶段 0：项目初始化

- [x] 创建 `.gitignore`（含 `node_modules/`、`__pycache__/`、`.venv/`、`backend/data/`、`dist/`、`.codegraph/`）
- [x] `git init` + 配置 user.name/email + 首次提交
- [x] 创建 `README.md`（启动方式、目录说明）
- [x] 创建项目级 skill `.opencode/skills/blueprint-design/SKILL.md`
- [x] `codegraph init`（若命令可用）
- [x] 建立 `backend/`、`frontend/` 目录骨架与 `__init__.py`

**验收**：`git status` 干净；目录结构符合工程设计 §2。

---

## 阶段 1：Python 文件扫描与 AST 解析

- [x] `backend/app/config.py`（忽略目录、recent_days 等常量）
- [x] `backend/app/scanner/project_scanner.py`
  - [x] `os.walk` + 忽略目录裁剪
  - [x] `path.read_bytes()` + `ast.parse` 容错
  - [x] `module_dotted` / `is_package` / `rel_path`(POSIX) 计算
  - [x] 逐文件跳过 + warnings 收集
- [x] `backend/app/scanner/python_parser.py`
  - [x] `ast.Import` / `ast.ImportFrom` → `ImportRef`
  - [x] 顶层 `functions` 提取
- [x] `backend/app/models/schemas.py`（中间 dataclass + Pydantic 模型）
- [x] `backend/tests/test_python_parser.py`
  - [x] `import a.b`、`from a import b`、`from . import c`、`from ..x import y` 用例
  - [x] 语法错误文件被跳过

**验收**：对样本目录运行扫描，得到正确的 `FileMeta` 列表；坏文件不中断；`pytest` 通过。

---

## 阶段 2：Git 历史读取

- [x] `backend/app/scanner/git_reader.py`
  - [x] `rev-parse` 仓库校验
  - [x] 单次 `git -c core.quotepath=false log --pretty=%x1e%H%x1f%aI%x1f%s --name-only`
  - [x] 按 RS/US 解析，从新到旧取每文件首次出现
  - [x] 异常 → `GitError`
- [x] `backend/tests/test_git_reader.py`
  - [x] 用 `tmp_path` 建临时 git 仓库，造 recent/old/未提交三态文件
  - [x] 中文文件名路径正确
  - [x] 无提交的新仓库返回空 dict

**验收**：临时仓库三态判定正确；中文路径无乱码；`pytest` 通过。

---

## 阶段 3：蓝图构图

- [x] `backend/app/scanner/blueprint_builder.py`
  - [x] 模块索引 `dotted → [rel_path]`
  - [x] `resolve()` 绝对/相对/包映射/别名（按工程设计 §4.4 伪码）
  - [x] 丢弃三方库边、自环、重复边
  - [x] 状态判定（aware datetime + recent_days）
  - [x] `is_isolated` 标记
- [x] `backend/tests/test_blueprint_builder.py`
  - [x] 绝对导入命中、相对导入命中、`__init__` 包映射
  - [x] 同名模块消歧
  - [x] 三方库被丢弃、自环被丢弃

**验收**：构造的小型多包样本能得到预期节点/边；`pytest` 通过。

---

## 阶段 4：接口、日志与错误处理

- [x] `backend/app/services/logging_db.py`（建表、写入、按时间/级别查询、CSV 导出）
- [x] `backend/app/services/scan_service.py`（编排 + 分阶段日志）
- [x] `backend/app/api/routes.py`（`/api/blueprint/scan`、`/api/health`、`/api/logs/export`）
- [x] `backend/app/main.py`（FastAPI 装配、CORS、统一异常处理器）
- [x] 错误分支：路径不存在/非目录/无 `.git`/git 失败（§5.6）
- [x] `backend/tests/test_scan_api.py`
  - [x] 四种错误分支返回正确状态码与信息
  - [x] 正常扫描返回符合 schema 的 JSON
- [x] `backend/requirements.txt`、`pytest.ini`

**验收**：`uvicorn` 启动；`curl`/httpx 调用四个错误分支与正常分支均正确；日志库出现对应记录。

---

## 阶段 5：后端端到端与性能基线

- [x] 用一个真实小项目跑通全链路
- [x] 用目标大项目（800+ 文件）运行，记录扫描/构图耗时（写入日志）
- [x] 端到端脚本 `backend/tests/e2e_scan.py`（可选 pytest 标记）

**验收**：链路跑通；记录实际耗时数据作为后续优化基线。

---

## 阶段 6：前端脚手架

- [x] `npm create vite`（React + TS）+ 依赖：`@xyflow/react`、`zustand`、`@dagrejs/dagre`、`tailwindcss`
- [x] Tailwind/PostCSS 配置、`index.css`
- [x] `services/api.ts`（fetch 封装 + 超时 + 错误透传）
- [x] `services/logger.ts`（IndexedDB）
- [x] `store/blueprintStore.ts`（Zustand）
- [x] `App.tsx` 骨架 + `ErrorBoundary`

**验收**：`npm run dev` 启动无报错；能调通 `/api/health`。

---

## 阶段 7：画布与节点

- [x] `layout/dagreLayout.ts`（dagre + 网格退化）
- [x] `components/BlueprintCanvas.tsx`（React Flow 容器）
- [x] `components/CircleNode.tsx`（圆圈 + 截断文件名 + Handle + 边框色）
- [x] `utils/commitStatus.ts`（状态→颜色）
- [x] `components/ProjectPathInput.tsx`（输入 + 扫描 + loading）

**验收**：输入目录后画布显示不重叠的圆圈节点；可缩放、拖拽、平移。

---

## 阶段 8：详情、搜索、高亮

- [x] `components/NodeTooltip.tsx`（悬停显示全部字段）
- [x] `components/SearchBar.tsx`（按文件名/路径过滤）
- [x] 点击节点 → 邻接表 BFS 上下游高亮 + 其余淡化
- [x] Esc 清除选中/搜索

**验收**：MVP 验收第 7/8/9 条与 4 个重点全部通过。

---

## 阶段 9：前端日志与错误态

- [x] `components/ErrorBanner.tsx`（后端错误透传，不白屏）
- [x] `components/LogExportPanel.tsx`（起止时间 + 级别 → 导出前后端日志）
- [x] 前端关键操作/异常全部写 IndexedDB

**验收**：断网/错误路径显示提示；能按时间区间导出前后端日志。

---

## 阶段 10：全量验收与收尾

- [x] 逐条核对 MVP 十条验收标准（工程设计 §10）
- [x] 记录 4 个重点验证结果
- [x] `pytest` 全绿
- [x] 防作弊审计（anti-cheating-review）PASS
- [x] 最终验收（verification-before-completion）PASS
- [x] 只暂存本对话改动文件并提交

**验收**：MVP 十条全过 + 双审计 PASS + 已提交。

---

## 验收命令速查

```bash
# 后端
cd backend
pip install -r requirements.txt
uvicorn app.main:app --reload --port 8000
pytest -q

# 前端
cd frontend
npm install
npm run dev
```

```bash
# 正常扫描
curl -X POST http://localhost:8000/api/blueprint/scan -H "Content-Type: application/json" -d "{\"project_path\":\"D:\\\\path\\\\to\\\\project\"}"

# 错误分支（无 .git）
curl -X POST http://localhost:8000/api/blueprint/scan -H "Content-Type: application/json" -d "{\"project_path\":\"C:\\\\Windows\"}"
```
